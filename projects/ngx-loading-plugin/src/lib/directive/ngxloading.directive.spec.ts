import { NgxloadingDirective } from './ngxloading.directive';

describe('NgxloadingDirective', () => {
  it('should create an instance', () => {
    // const directive = new NgxloadingDirective();
    // expect(directive).toBeTruthy();
  });
});
